<Window x:Class="StudentsManager.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:StudentsManager.Desktop"
        
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        
        <ListBox x:Name="ListOfStudents"
                 Grid.Column="0"/>
        
        <StackPanel Orientation="Vertical"
                    Grid.Column="1">
            <local:InputComponent x:Name="InputId"
                                  LabelText="ID"/>
            <local:InputComponent x:Name="InputLastName"
                                  LabelText="Фамилия"/>
            <local:InputComponent x:Name="InputFirstName"
                                  LabelText="Имя"/>
            
            <StackPanel Orientation="Horizontal">
                <Button Content="Сохранить"/>
                <Button Content="Удалить"/>
                <Button Content="Очистить"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
